Build a minimal Alpine rootfs fixture for OpenRC boot testing.

- [x] Define the rootfs source and reproducibility contract.
- [x] Build or import a minimal Alpine userspace with BusyBox, musl, OpenRC, and base config.
- [x] Install Bunix's native `/bin/login` in the fixture and configure login/getty paths to use it, because Bunix login must RPC to the user server.
- [x] Package the fixture without GPL contamination in the kernel or servers; GPL userspace is acceptable.
- [x] Teach the build/test harness how to select the Alpine rootfs fixture separately from the current synthetic rootfs.
- [x] Add a smoke test that can inspect `/etc/alpine-release`, `/sbin/init`, OpenRC config, and required symlinks.
- [x] Document how to refresh the fixture.
