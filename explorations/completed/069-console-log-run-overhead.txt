Reduce console/log overhead on interactive runs.

Parent exploration: `068-make-run-latency-baseline.txt`

Problem:

- `make run` defaults to `log=info`, and `-serial stdio` makes every emitted
  line part of the interactive critical path.
- The logger already demotes many scheduler, IPC, buffer, and task lifecycle
  lines to trace, but some expensive diagnostic paths still emit at info/error
  during normal boot.
- `arch/x86_64/user.c` negative Linux syscall dumps print multiple stack,
  byte, and register lines through `console_printf()`.  These are useful when
  debugging syscall personality bugs, but they are not acceptable as default
  run-path noise.
- Server logs should remain available through dmesg/ring-buffer diagnostics
  without slowing the controlling console once real userspace is up.

Goal:

- Make normal `make run` quiet and fast while preserving explicit debug and
  strace-style observability when requested.

Tasks:

- [x] Use the baseline from exploration 068 to identify all high-volume log
      prefixes emitted before and after Linux userspace starts.
- [x] Move `user-syscall:` negative syscall backtrace dumps behind an explicit
      debug/trace flag or `log=debug`, and keep a compact single-line warning
      only when needed.
- [x] Audit info-level server logs and demote routine readiness/chatter to
      trace while keeping boot-critical milestones and errors visible.
- [x] Ensure scheduler, IPC, buffer, VM, and VFS diagnostic prefixes are
      consistently classified by `kernel/console.c`.
- [x] Keep `log=debug` and Linux server strace behavior available for focused
      debugging without changing default `make run`.
- [x] Add a regression or smoke check that default run logs do not contain
      repeated high-volume diagnostic prefixes before login.
- [x] Re-profile `make run` and record the latency/output delta.

Baseline from exploration 068:

- Default Alpine networking profile
  `build/run-profile/20260712T232619Z-17014`:
  - Serial output before poweroff: 357 lines, 17620 bytes.
  - Top prefixes: `kernel` 84, `names` 82, `vm-server` 35, `bootstrap` 14,
    `vfs` 13, `pci` 8, `netcfg` 8, `virtio-net` 7, `virtio-bus` 6.
  - Login prompt appeared at 40126 ms, while `bootstrap: linux init exec`
    appeared at 7071 ms.
- Synthetic comparison profile
  `build/run-profile/20260712T232231Z-14525`:
  - Serial output before poweroff: 420 lines, 22293 bytes.
  - Top prefixes: `names` 93, `kernel` 83, `user-syscall` 52, `vm-server` 33,
    `sched` 20.
- First read:
  - Console/log volume is real and should be reduced, especially routine
    `kernel`, `names`, `vm-server`, and `user-syscall` chatter.
  - The largest default `make run` latency gap is not explained by serial byte
    volume alone: the repeated profile showed a roughly 33s gap between Linux
    init handoff and login prompt with only 17 KB of serial output.  This
    exploration should still clean log overhead, while exploration 070 should
    treat OpenRC/login/networking startup as the primary boot critical-path
    suspect.
- Classified default Alpine networking profile
  `build/run-profile/20260712T233726Z-21452`:
  - Total serial output before poweroff: 360 lines, 17693 bytes.
  - Kernel/server bringup emitted 319 lines and 15671 bytes.  Top categories:
    `kernel` 89 lines, `names` 85, `vm` 35, `vfs-fs` 32, `networking` 17,
    `drivers-bus` 16, `sched-irq` 15, `linux-compat` 6, `ipc-buffer` 2.
  - The Linux-init-to-login slow path emitted only 19 lines and 640 bytes:
    `openrc` 14 lines, `vm` 1, `login-shell` 1, plus 3 blank lines.
  - Slow-path class totals were `guest-stdout` 15 lines / 573 bytes,
    Bunix `info` 1 line / 64 bytes, and blank `other` 3 bytes.
  - Default Alpine `log=info` did not emit `user-syscall:` in this run.
    Synthetic shell boot remains the measured `user-syscall:` offender, with
    52 lines before login in `build/run-profile/20260712T232231Z-14525`.
- Refined read:
  - This exploration should still quiet kernel/server bringup and
    `user-syscall:` dumps, because they dominate the serial volume.
  - It should not be expected to remove the entire 31-34s default login delay
    by itself: the measured slow path is mostly silent.

Progress:

- `arch/x86_64/user.c` now emits only a compact default warning for negative
  native/Linux syscall misuse and leaves the detailed `user-syscall:` byte,
  stack, frame, and argument dump behind console verbosity classification.
- `kernel/console.c` classifies additional VM grant/allocation chatter,
  `user-syscall:` dumps, and copied-module accounting below default
  `log=info`, while preserving boot-critical module launch/start milestones.
- `tools/check-run-profile-log-volume.sh` and `make
  test-run-profile-log-volume` enforce the current default run-output budget.
- Safe reduction profile `build/run-profile/20260713T000744Z-7395`:
  - Serial output fell from 360 lines / 17693 bytes to 297 lines / 12433
    bytes.
  - Kernel/server phase fell from 319 lines / 15671 bytes to 257 lines /
    10538 bytes.
  - `vm-server` dropped from 35 prefix lines to 1; `user-syscall` remained 0
    in the default Alpine run.
  - Login prompt still appeared late, at 34410 ms, so console output was not
    the primary cause of the slow default run.
- Fresh validation profile `build/run-profile/20260713T001856Z-15158`:
  - Reached `bootstrap: fs ready` at 5761 ms and `bootstrap: linux init exec`
    at 5765 ms.
  - Reached `login:` at 37819 ms, `~ #` at 39825 ms, and poweroff at 42320 ms.
  - Emitted 297 lines / 12432 bytes, with only 18 lines / 576 bytes during
    the Linux-init-to-login interval.
  - This confirms the remaining user-visible delay is the silent
    Alpine/OpenRC/login path, not serial output.
- Latest validation profile `build/run-profile/20260713T002648Z-19685`:
  - Reached `bootstrap: fs ready` at 5084 ms and `bootstrap: linux init exec`
    at 5086 ms.
  - Reached `login:` at 50159 ms, `~ #` at 51163 ms, and poweroff at 53620 ms.
  - Emitted 297 lines / 12433 bytes, with only 18 lines during the
    Linux-init-to-login interval.
  - This repeats the same diagnosis: normal `make run` is now dominated by
    silent Alpine/OpenRC/login startup after Linux init starts.
- Server-log audit profile `build/run-profile/20260713T003404Z-24673`:
  - User-space server log lines now go through line-level verbosity
    classification in the console sink.  Demoted lines still enter the dmesg
    ring, but routine `names:` resolver chatter and translator mount chatter
    no longer print to serial at default `log=info`.
  - Serial output fell from 297 lines / 12433 bytes to 191 lines / 9364 bytes.
  - Kernel/server phase output fell from 257 lines / 10538 bytes to 151 lines
    / 7575 bytes.
  - `names:` fell from 86 prefix lines to 3, while `vfs:` fell from 13 to 2
    and single-line `tmpfs`, `devfs`, `sysfs`, `utmpfs`, and `unionfs`
    online markers remained visible.
  - Reached `bootstrap: fs ready` at 3077 ms, `bootstrap: linux init exec` at
    3079 ms, `login:` at 47148 ms, `~ #` at 48153 ms, and poweroff at
    50688 ms.
  - This confirms the remaining delay is still the silent
    Alpine/OpenRC/login path, while the default serial path is now much
    quieter.
- Important boot-order finding:
  - A broad raw-log filter that suppressed routine server text, and a separate
    attempt to demote `kernel: launching module server` /
    `kernel: starting module server`, both made default `make run-profile`
    hang before `bootstrap: fs ready`.
  - The same broad filter succeeded with `log=debug`.  That points to an
    accidental serial/log pacing dependency in early server launch ordering,
    not a legitimate requirement for visible console output.
- Validation/probe notes:
  - `sh -n tools/check-run-profile-log-volume.sh` passed.
  - `sh tools/check-run-profile-log-volume.sh
    build/run-profile/20260713T000744Z-7395` passed with
    `serial_lines=297`, `serial_bytes=12433`, `kernel_prefix=53`,
    `vm_server_prefix=1`, `user_syscall_prefix=0`, and
    `kernel_server_phase_lines=257`.
  - `make run-profile` passed for
    `build/run-profile/20260713T001856Z-15158`, and the log-volume checker
    passed with `serial_lines=297`, `serial_bytes=12432`, `kernel_prefix=53`,
    `vm_server_prefix=1`, `user_syscall_prefix=0`, and
    `kernel_server_phase_lines=257`.
  - `make run-profile` passed for
    `build/run-profile/20260713T002648Z-19685`, and the log-volume checker
    passed with `serial_lines=297`, `serial_bytes=12433`,
    `kernel_prefix=53`, `vm_server_prefix=1`, `user_syscall_prefix=0`, and
    `kernel_server_phase_lines=257`.
  - `make test-boot` passed after demoting routine server log lines and
    updating marker files to assert boot milestones rather than debug chatter.
  - `make run-profile` passed for
    `build/run-profile/20260713T003404Z-24673`, and the log-volume checker
    passed with `serial_lines=191`, `serial_bytes=9364`,
    `kernel_prefix=53`, `names_prefix=3`, `vm_server_prefix=1`,
    `user_syscall_prefix=0`, and `kernel_server_phase_lines=151`.
  - `make test-boot` now passes after the synthetic proc-spawn path was fixed
    under exploration 070 by registering native proc-spawned tasks with the
    user server before their VFS executable opens.

Completion:

- Full-test completion gate passed with `make test`, run
  `build/test-runs/20260713T022430Z-12425`.
- All 29 shards passed, including the previously failing Alpine OpenRC marker
  shards, `root-login-union`, `scheduler-migration`, and `login-smoke`.

High-priority slowdown series:

1. `069-console-log-run-overhead`: reduce default serial volume and add a
   regression budget so console noise does not keep creeping back.
2. `070-boot-service-critical-path`: remove the accidental log-pacing race and
   replace silent waits/polling with explicit readiness and timing attribution.
3. `071-developer-run-profile`: split fast development runs from faithful
   Alpine/OpenRC/networking validation so `make run` can become quick without
   losing the full-system profile.
