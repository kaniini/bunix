Make BusyBox wget work through the normal Alpine resolver and TCP path.

Follow-up:

- This is the focused follow-up split out from
  `completed/054a-ip-stack-end-to-end-connectivity.txt`.
- Exploration 054a fixed the earlier post-DHCP IP stack failures enough to
  validate external ICMP and BusyBox `nslookup` through `/etc/resolv.conf`.
- Do not restart broad IP-stack debugging here unless evidence from `wget`
  points back to packet delivery.  The first assumption to test is that
  BusyBox `wget` exercises a different musl resolver or Linux socket API path
  than BusyBox `nslookup`.

Problem:

- BusyBox `nslookup example.com` works from Alpine userspace using the
  generated `/etc/resolv.conf` path.
- BusyBox `wget http://example.com` still fails with:
  `wget: bad address 'example.com'`.
- Because `nslookup` succeeds, this is probably not a generic DHCP, link,
  ICMP, or `/etc/resolv.conf` failure.  It is likely in musl
  `getaddrinfo()`, file lookup behavior, unsupported Linux networking
  syscalls, socket option compatibility, nonblocking semantics, or subsequent
  TCP connect/send/recv behavior.

Required failure case:

- `busybox wget -O /tmp/w http://example.com` must resolve `example.com`,
  establish a TCP connection, retrieve content, and leave a non-empty
  `/tmp/w`.

Intended outcome:

- A fresh `make run` Alpine environment boots, logs in, brings networking up,
  and can run:
  - `busybox nslookup example.com`
  - `busybox wget -O /tmp/w http://example.com`
  - `test -s /tmp/w`
- The HTTP validation uses DNS and does not substitute a pre-resolved address.

Constraints:

- Do not special-case `example.com`, DNS, or HTTP in the test harness.
- Keep DNS policy in userspace and packet mechanics in the network servers.
- Preserve the working external ping and `nslookup` behavior from exploration
  054a.
- Account for missing or wrong Linux APIs by stracing `wget` under
  `log=debug` instead of guessing from packet behavior alone.

Investigation targets:

- Trace `busybox wget http://example.com` under structured Linux strace and
  identify all unsupported or suspicious syscalls before changing networking
  internals.
- Compare the syscall path used by BusyBox `nslookup` with the path used by
  BusyBox `wget` and musl `getaddrinfo()`.
- Verify resolver input file behavior for `/etc/resolv.conf`, `/etc/hosts`,
  `/etc/nsswitch.conf`, and any musl-specific fallbacks.
- Verify `socket()`, `connect()`, `sendto()`, `recvfrom()`, `poll()`,
  `fcntl()`, `getsockopt()`, and `setsockopt()` behavior used by `wget`.
- Verify TCP connect/send/recv/close behavior against an external HTTP server
  after name resolution succeeds.

Findings:

- A debug probe reproduced `busybox wget -O /tmp/w http://example.com` failing
  with `wget: bad address 'example.com'` after the real Alpine OpenRC
  networking service started `lo` and `eth0` and BusyBox/Alpine `udhcpc`
  obtained `10.0.2.15` from QEMU user networking.  The current evidence keeps
  this focused on the `wget` resolver/socket path rather than replacing
  `/etc/init.d/networking`.
- The latest debug artifact is
  `build/failures/20260713T095339Z-1732/20260713T095645Z-1732`.
- The resolver failure was not a DHCP, `/etc/resolv.conf`, or OpenRC service
  problem.  Musl's `getaddrinfo()` path used UDP receive calls that expected a
  source sockaddr.  The old Bunix path could return the DNS payload but did not
  preserve the native source endpoint for Linux `recvfrom()`/`recvmsg()`
  callers.
- After UDP source sockaddr reporting was fixed, `wget` resolved
  `example.com` and reached `Connecting to example.com (...:80)`, then failed
  while waiting for the HTTP response.  That exposed the next bug: outbound TCP
  connect sent SYN and marked the socket established, but the net server did
  not process external SYN-ACK/data/FIN/RST packets and `TCP_WRITE` only knew
  how to deliver to local peer sockets.
- The net server now has an initial outbound IPv4 TCP client path: per-socket
  sequence/ack tracking, queued writes until SYN-ACK, ACK processing, payload
  receive queues, FIN/RST handling, and correct would-block behavior for TCP
  reads before data arrives.
- The normal Alpine DNS/wget gate now passes through the real Alpine OpenRC
  networking service.  `make test-boot-virtio-net-dns-wget` validates
  `/run/openrc/started/networking`, `rc-service networking status`, resolver
  state from DHCP, BusyBox `nslookup`, BusyBox `wget http://example.com`, and a
  non-empty fetched output file.

Tasks:

- [x] Add or repair a focused guest validation for `busybox wget
      http://example.com` that is not vulnerable to serial command-file
      corruption.
- [x] Attempt structured Linux syscall tracing for the failing `wget` path and
      fall back to focused syscall/API isolation when the serial/ring-buffer
      trace did not capture useful networking calls.
- [x] Identify the first incorrect syscall result or unsupported API on the
      failing path.
- [x] Fix the resolver or socket compatibility issue that causes
      `wget: bad address 'example.com'`.
- [x] If resolution succeeds but HTTP still fails, fix the next TCP
      connect/send/recv compatibility issue exposed by `wget`.
- [x] Validate `busybox wget -O /tmp/w http://example.com` end to end with DNS
      enabled and `test -s /tmp/w`.
- [x] Re-run the external ping and DNS validations from exploration 054a to
      prove no regression.
- [x] Update notes with any remaining networking limits.

Remaining limits:

- The TCP work here is an MVP outbound IPv4 client path for simple HTTP-style
  flows.  It does not yet implement retransmit timers, congestion control,
  out-of-order reassembly, PMTU/MSS negotiation, window scaling, or a complete
  close state machine.
- IPv6 external TCP connect still only covers the older SYN emission smoke;
  IPv6 data receive/send parity should be a separate networking-stack
  exploration.

Completion criteria:

- `busybox wget -O /tmp/w http://example.com` succeeds from the guest using
  DNS.
- `/tmp/w` is non-empty and contains HTTP content from the remote server.
- Existing external ping and BusyBox `nslookup` validations still pass.
- The focused HTTP validation is automated enough to be run without relying on
  fragile long serial command-file injection.

Validation:

- `make build/modules/net.server`
- `make build/modules/linux.server`
- `make build/modules/nettest.user`
- `make build/x86_64/bunixos.kernel`
- `BUNIX_USER=root BUNIX_PASSWORD=root BUNIX_PROMPT='~ # ' BUNIX_CMD='/bin/nettest' BUNIX_MARKER=NETTEST_OK make test-command`
- `make test-boot-virtio-net-dns-wget`
- `make test-boot-virtio-net-external-ping-strict`
