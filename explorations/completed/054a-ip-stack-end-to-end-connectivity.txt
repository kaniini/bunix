Fix IP stack correctness through DNS resolution.

Follow-up:

- This exploration runs after `054-stock-alpine-networking-service.txt` is
  either completed or explicitly postponed.
- This ran before VFS-backed mmap/SIGBUS and later unrelated runtime work,
  because a default Alpine boot is not useful until the configured network is
  usable enough from userspace to prove link, DHCP, ICMP, and DNS.
- Exploration 054 is about getting Alpine networking initialized by the
  normal OpenRC/ifupdown-ng service path.  This exploration is the next slice
  for proving that the IP stack itself is correct once the interface is up.
- Exploration 054 is currently postponed on an OpenRC `/run/openrc/deptree`
  runtime-state failure.  Until that is resumed, run this exploration against
  the default generated Bunix networking service path.  Do not treat that as
  completion of the stock OpenRC service goal; it is only the current way to
  obtain a configured interface while debugging IP-stack correctness.
- The default generated-service Alpine image has been restored to a minimal
  generated OpenRC policy, so this exploration can proceed against the
  generated networking path while stock OpenRC networking remains postponed in
  exploration 054.

Problem:

- The current default Alpine networking path can bring up interface
  configuration, but the IP stack had serious correctness gaps after DHCP
  succeeded.
- This exploration is specifically for the post-DHCP "network is up but the
  stack is broken" state observed from a normal Alpine shell.
- The immediate follow-up to stock OpenRC networking was to examine why the IP
  stack was still broken under normal Alpine userspace, not to add more
  boot-time networking policy.
- This exploration existed to examine why the IP stack was still broken once
  link and DHCP appeared to be working, instead of treating DHCP success as
  proof of usable networking.
- The IP stack appeared substantially broken after link and DHCP success:
  ping TTLs were inconsistent, ping replies could be attributed to the wrong
  source address, and DNS did not work.
- Current reported failures to reproduce and fix:
  - Ping TTLs are all over the place.
  - DNS does not work.
- Reported external ping behavior includes replies from `4.2.2.1` appearing
  as `0.0.0.0`, missing replies after the first packet, and later
  `recvfrom: Resource temporarily unavailable` errors.
- Reported higher-level failures include DNS resolution failing from Alpine
  userspace.
- ICMP replies had incorrect or unstable TTL/source metadata, which meant
  receive-side packet attribution, route lookup, packet header handling, or
  ICMP socket delivery was not trustworthy enough yet.
- DNS resolution did not work from Alpine userspace.
- `wget http://example.com` still does not work after DNS was fixed.  That is
  now tracked separately as exploration `078-wget-resolver-http-compatibility.txt`
  so this completed IP/DNS slice does not keep expanding into every Linux
  resolver or TCP compatibility issue.

Required failure cases:

- `ping` must no longer report wildly inconsistent TTLs for the same remote
  endpoint.
- `ping -c 4 4.2.2.1` must produce four replies attributed to `4.2.2.1`,
  not `0.0.0.0`, and must not degrade into repeated `recvfrom: Resource
  temporarily unavailable` errors after the first reply.
- DNS resolution must work through Alpine's normal resolver path.
- DNS checks must run after the interface has been initialized by
  the default Alpine boot networking path, not through a manually preconfigured
  test shortcut.  While exploration 054 is postponed, that default path is the
  generated Bunix networking service; once stock OpenRC networking is restored,
  these checks should be revalidated through the stock service path too.

Intended outcome:

- A fresh `make run` Alpine environment boots, logs in, and has usable
  userspace networking through the normal configured interface path for DHCP,
  ICMP, and DNS.
- IPv4 ICMP replies report the real remote source address and sane TTL values.
- DNS works through Alpine's normal resolver configuration.
- DNS validation runs from the guest's normal Alpine userspace
  environment after OpenRC networking has brought the interface up.
- The validation exercises the real userspace stack rather than a synthetic
  kernel-only shortcut.

Constraints:

- Do not write a new DHCP client; use the Alpine/BusyBox userspace already in
  the image.
- Do not special-case `example.com`, DNS, or ICMP in the test harness.
- Keep network policy in userspace and keep packet mechanics in the network
  servers.
- Preserve localhost IPv4/IPv6 behavior while fixing external networking.
- Treat incorrect TTL/source reporting as a stack correctness bug, not a
  display-only problem.
- Do not call the exploration complete just because DHCP succeeds.  It must
  also prove ICMP and DNS through normal Alpine userspace commands.

Investigation targets:

- Audit IPv4 receive dispatch from virtio-net through the network server into
  Linux socket wakeups.
- Verify that qemu/virtio-net receive packets are delivered to the IP stack and
  then to the correct userspace socket, with no stale packet metadata reused
  between receives.
- Verify ICMP echo reply parsing, source-address selection, TTL propagation,
  route lookup, and socket receive metadata.
- Verify UDP sockets used by DNS, including ephemeral port binding, reply
  demultiplexing, timeouts, and `recvfrom()`/`poll()` semantics.
- Verify resolver inputs: `/etc/resolv.conf`, DHCP-provided nameservers, and
  any Bunix-generated fallback configuration.
- Trace the packet path far enough to prove whether bad TTL/source values are
  introduced by virtio-net receive, IPv4 parsing, ICMP handling, route/socket
  metadata, or Linux `recvfrom()` translation.

Tasks:

- [x] Add a deterministic guest test for external IPv4 ping that verifies the
      remote source address and sane TTL values for multiple replies.
- [x] Reproduce and fix the reported ping behavior where replies from
      `4.2.2.1` are displayed as coming from `0.0.0.0` or have inconsistent
      TTLs.
- [x] Reproduce and fix the reported follow-on ping failures where later echo
      replies produce `recvfrom: Resource temporarily unavailable` instead of
      four successful replies.
- [x] Add a deterministic DNS guest test using Alpine's normal resolver path.
- [x] Make DNS resolution work from BusyBox/Alpine userspace.
- [x] Add a deterministic HTTP guest test using BusyBox `wget
      http://example.com`.
- [x] Move the remaining `wget http://example.com` failure into a focused
      follow-up exploration for resolver, socket, and TCP compatibility.
- [x] Validate DNS end to end from the guest by resolving a public hostname
      through Alpine's normal resolver configuration.
- [x] Validate that the fixes work after a fresh Alpine boot path, not only in
      a handcrafted debug image.
- [x] Update networking documentation or exploration notes with the remaining
      known limits after DNS and HTTP work.

Completion criteria:

- `make run` boots to an Alpine login with networking initialized.
- `ping -c 4 4.2.2.1` returns four replies from `4.2.2.1` with plausible TTLs.
- DNS resolution works from the guest using normal resolver configuration.
- DNS is validated end to end in the guest after OpenRC networking has
  initialized the interface.
- Automated tests cover ping correctness and DNS.  HTTP fetch behavior is
  tracked by exploration `078-wget-resolver-http-compatibility.txt`.

Implementation Notes:

- Added `tools/guest-network-external-ping.sh` and
  `make test-boot-virtio-net-external-ping-strict`.  The guest script checks
  that default Alpine networking is up, runs `ping -c 4 -W 4 4.2.2.1`, rejects
  replies attributed to `0.0.0.0`, requires four replies from `4.2.2.1`, and
  requires all four TTL values to be in range and stable across replies.
- Added `tools/guest-network-dns-wget.sh` and
  `make test-boot-virtio-net-dns-wget`.  The guest script checks default
  Alpine networking, verifies `/etc/resolv.conf`, resolves `example.com` with
  BusyBox `nslookup`, then fetches `http://example.com` with BusyBox `wget`.
- Added `make test-boot-virtio-net-external-stack` as a convenience target for
  running the strict ping and DNS/wget checks sequentially.
- These targets currently cannot reach their guest assertions because Alpine
  previously failed before login in the OpenRC `/run/openrc/deptree` path.
  After restoring generated mode to a minimal OpenRC policy, the strict ping
  target reaches login and runs the guest assertions.
- Fixed the external IPv4 ping source/TTL path.  The net server now carries
  the IPv4 TTL from received ICMP packets, and the Linux server preserves
  socket type so raw IPv4 ICMP sockets receive a synthetic IPv4 header plus
  ICMP payload instead of interpreting the ICMP payload as an IP header.  This
  fixes the observed drifting TTL values.
- `make test-boot-virtio-net-external-ping-strict` now passes.  The target
  validates four replies from `4.2.2.1` and requires all four reply lines to
  report `ttl=255` under QEMU user networking.
- `tools/guest-network-external-ping.sh` now accumulates failure state and
  returns a failing final command when source, count, or TTL assertions fail,
  which avoids masking failures when the harness types command files into an
  interactive shell.
- The serial command-file path is still fragile for long scripts.  The strict
  ping target now uses a compact one-line command instead of
  `BUNIX_CMD_FILE`, because command-file injection can overrun the shell input
  path while long-running commands such as `ping` are still executing.  The
  next test-efficiency slice should replace typed command files with a real
  guest-side script transport or module-backed runner instead of further
  tuning serial pacing.
- A DNS/wget probe now reaches the guest and fails at the expected resolver
  layer.  `/etc/resolv.conf` is missing, BusyBox `nslookup example.com` falls
  back to `127.0.0.1` and reports `write to '127.0.0.1': Bad file descriptor`,
  and `wget http://example.com` fails with `bad address 'example.com'`.
  Artifact: `build/failures/20260711T105731Z-20972/20260711T110331Z-20972`.
  The DNS/wget guest script also masked failures and needed the same
  final-status hardening as the strict ping test.
- Hardened `tools/guest-network-dns-wget.sh` so it accumulates failures and
  only prints `BUNIX_NET_DNS_WGET_OK` when every resolver and wget assertion
  succeeds.
- Generated fallback Alpine networking now publishes `/etc/resolv.conf`
  through VFS from the netcfg server, using QEMU user networking's DNS server
  `10.0.2.3`.  A short guest validation passed:
  `cat /etc/resolv.conf; grep -F "nameserver 10.0.2.3" /etc/resolv.conf`.
- Fixed Linux `write(2)` on connected UDP sockets by forwarding UDP sockets
  through the existing `linux_sendto()` path.  This removes the BusyBox
  `nslookup` failure `write to '10.0.2.2': Bad file descriptor`.
- BusyBox `nslookup example.com` now works from Alpine userspace through the
  generated `/etc/resolv.conf` path.  Short guest validation passed:
  `busybox nslookup example.com >/tmp/d 2>&1; cat /tmp/d; grep -F "Name:"
  /tmp/d; grep -F "Address" /tmp/d`.
- BusyBox `wget http://example.com` still fails with `bad address
  'example.com'` even after `nslookup` works, so the remaining HTTP task is
  now specifically wget's resolver/API path and subsequent TCP/HTTP behavior.
  Artifact: `build/failures/20260711T115839Z-9259/20260711T120444Z-9259`.
- The remaining `wget` failure was split out deliberately instead of
  continuing to broaden this exploration.  The evidence says this slice fixed
  DHCP-adjacent IP correctness and DNS, while `wget` is now a narrower Linux
  resolver, socket API, or TCP compatibility issue.
- `make test-boot-virtio-net-dns-wget` is still not a trustworthy full-stack
  validator because the current serial command-file injection path can corrupt
  long guest scripts before execution.  The DNS fixes above were validated
  with short `BUNIX_CMD` commands; replacing typed command-file upload remains
  part of the test-efficiency work.
- Validation for the test scaffolding:
  - `git diff --check`
  - `make test-alpine-rootfs`
  - `make test-alpine-rootfs-stock-networking`
  - `make build/modules/net.server`
  - `make build/modules/linux.server`
  - `make build/modules/netcfg.server`
  - `BUNIX_SHELL_PART=network-loopback-ping make test-shell-part`
  - `make test-boot-virtio-net-external-ping-strict`
  - short Alpine guest `/etc/resolv.conf` validation against
    `nameserver 10.0.2.3`
  - short Alpine guest `busybox nslookup example.com` validation
