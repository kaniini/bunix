Build the remaining multiuser/OpenRC base in small slices.

Poweroff and deterministic test exit:

- [x] Define a guest poweroff contract for tests using ACPI S5 shutdown.
- [x] Add a native machine shutdown syscall with an x86_64 arch power backend.
- [x] Make the Linux server implement `reboot(..., LINUX_REBOOT_CMD_POWER_OFF)`.
- [x] Add BusyBox `poweroff`, `halt`, and `reboot` symlinks to the rootfs image.
- [x] Add a focused poweroff test path that waits for QEMU to exit through the guest.
- [x] Migrate shell/command tests to request guest poweroff after success.
- [x] Migrate boot tests away from timeout-as-success.

OpenRC/Alpine boot preparation plan:

- [x] Write follow-up explorations for building a minimal Alpine rootfs fixture.
- [x] Write follow-up explorations for remaining devfs/procfs/sysfs surfaces OpenRC expects.
- [x] Write follow-up explorations for Linux compatibility gaps discovered by OpenRC startup.
- [x] Write follow-up explorations for booting `/sbin/init` from Alpine and capturing service logs.
- [x] Write follow-up explorations for sharded OpenRC boot regression coverage.
