Fix binary command-output corruption in shell test markers.

Parent exploration: `064-vm-execute-permission-api.txt`

Problem:

- Full `make test` for exploration 064 reached the expected VM execute
  permission coverage, but failed two shell shards because command output was
  replaced by binary bytes.
- `procfs-sysfs-surface` failed `DMESG_PREFIX_OK`: `busybox dmesg` wrote binary
  bytes instead of a line matching the expected uptime-prefixed log format.
- `large-io-mount` failed `MNT_OK`: `mount -t tmpfs tmpfs /mnt && echo MNT_OK`
  mounted tmpfs successfully and later `cat`, `mount`, and `umount` checks
  passed, but binary bytes appeared where `MNT_OK` should have been printed.

Why this matters:

- The failing operations appear to succeed, so this is likely output-path,
  console-ring, dmesg, TTY, or harness synchronization corruption rather than
  the underlying VFS operation failing.
- The corruption blocks full-suite validation for VM execute permissions and
  will make future shell-marker failures ambiguous.

Plan:

- Reproduce `procfs-sysfs-surface` and `large-io-mount` independently with
  `log=debug` and capture serial, dmesg, and linux strace evidence around the
  corrupted writes.
- Trace the bytes emitted by `busybox dmesg` and by the `mount && echo MNT_OK`
  command path from Linux `write(2)` through the Linux server, TTY/console
  handling, consoled, and early-console fallback.
- Determine whether the bytes originate in guest userspace, Linux-server write
  handling, the console ring, serial output, or host-side harness capture.
- Fix the corruption at the owning layer without weakening marker checks.
- Re-run the two failed shards independently, then re-run full `make test` for
  exploration 064.

Tasks:

- [x] Reproduce both marker corruptions with focused shards and debug logging.
- [x] Identify the first layer where expected marker bytes become binary data.
- [x] Fix the output corruption at the owning layer.
- [x] Add focused regression coverage for the corrupted path.
- [ ] Re-run `procfs-sysfs-surface`, `large-io-mount`, and full `make test`.

Progress:

- Focused reruns of `procfs-sysfs-surface`, `large-io-mount`, and the paired
  `procfs-sysfs-surface,large-io-mount` shard passed.
- Full `make test` run `20260712T183347Z-995` passed the original
  `procfs-sysfs-surface` and `large-io-mount` shards.
- The same full run failed two different shards:
  - `procfs-self-pipe`: `/bin/procfspipetest` hung after
    `PROCFSPIPETEST_BEGIN_PS_5`.
  - `root-login-union`: reading `/.upper/usr/share/bunix/nested/created.txt`
    printed the expected payload followed by binary bytes, so
    `UNION_LOWER_PARENT_UPPER_OK` was not emitted.
- The `root-login-union` failure shows the same binary-tail symptom as the old
  marker failures, but now points more directly at VFS/unionfs/read forwarding
  or syscall-buffer handling than at console output itself.
- The repeated binary tail decodes as the Linux termios structure. The owning
  layer is the x86_64 Linux syscall forwarding path: `ioctl(TCSETS*)` and
  network-interface ioctl input staging used the global `syscall_copy_buffer`
  without `syscall_copy_lock`, so concurrent syscall forwarding could copy
  termios bytes into unrelated shared-buffer payloads under SMP/load.
- Patched the ioctl forwarding path to hold `syscall_copy_lock` across the
  read-from-user plus buffer-write staging operations.
- Focused post-fix checks passed:
  - `make test-shell-part BUNIX_SHELL_PART=root-login-union`
  - `make test-shell-part BUNIX_SHELL_PART=procfs-self-pipe`
  - `make test-shell-part BUNIX_SHELL_PART=procfs-sysfs-surface,large-io-mount`
- Full post-fix `make test` run `20260712T191102Z-23322` passed 28 of 29
  shards, including `root-login-union`, `procfs-sysfs-surface`, and
  `large-io-mount`. The remaining failure is now isolated to
  `procfs-self-pipe`, which hangs intermittently inside `/bin/procfspipetest`
  during repeated `/proc/self/*` pipelines.
- `procfs-self-pipe` passes when run through each focused path:
  - `make test-shell-part BUNIX_SHELL_PART=procfs-self-pipe`
  - `QEMU_TIMEOUT=600s make test-shell-part BUNIX_SHELL_PART=procfs-self-pipe`
  - `BUNIX_TEST_SET=procfs-self-pipe BUNIX_TEST_JOBS=8 sh tools/test-parallel.sh`
  - `make test BUNIX_TEST_SET=procfs-self-pipe`
- Full post-fix `make test` run `20260712T194159Z-9436` passed 28 of 29
  shards. The original affected shards passed:
  - `procfs-sysfs-surface`: passed in 132s.
  - `large-io-mount`: passed in 25s.
  - `procfs-self-pipe`: passed in 71s.
  - `root-login-union`: passed in 42s.
  - `linux-signaltest`: passed in 21s.
- The only remaining full-suite failure in that run was unrelated to the binary
  marker corruption: `tmpfs-extended` returned to `login:` after
  `busybox mv /tmp/bunix-link-src.txt /tmp/bunix-link-renamed.txt`, so the next
  harness command was sent to the login prompt instead of the shell.
- Follow-up exploration `082-tmpfs-extended-session-drop.txt` tracks the new
  full-suite blocker separately.
