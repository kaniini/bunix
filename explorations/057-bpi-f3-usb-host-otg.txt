Bring up BPI-F3 USB host and USB-C OTG controller paths.

Status:

- Planned.  Start after exploration 035 proves the BPI-F3 first hardware boot
  milestone.  Coordinate this with the generic USB HID and USB mass-storage
  explorations so controller work does not duplicate class-driver work.

Problem:

- BPI-F3 exposes USB 3.0 host ports and USB-C OTG.  Bunix already has planned
  multiserver USB work, but the board port must bind the actual K1 controller,
  clocks, resets, PHYs, IRQs, and role-switch behavior described by the
  firmware DT.

Tasks:

- [ ] Capture USB controller, PHY, regulator, clock, reset, and interrupt DT
      nodes from real BPI-F3 hardware.
- [ ] Identify whether the first controller backend should target xHCI,
      DWC3, or a vendor-specific wrapper.
- [ ] Add the BPI-F3 controller binding path to the userspace USB controller
      server design.
- [ ] Bring up host-mode enumeration with a low-risk device.
- [ ] Prove USB HID keyboard input once the generic HID exploration is ready.
- [ ] Prove USB mass-storage block access once the generic mass-storage
      exploration is ready.
- [ ] Plan OTG/device-mode support separately if the controller requires a
      different server policy.
