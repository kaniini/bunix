First, build a series of explorations to build out a working `poweroff` command, and then
migrate the tests to use it (so we do not have to wait on timeouts anymore).

After this, build a series of explorations to build the remaining components necessary
in order to achieve an Alpine base image booting with OpenRC.
