Restore the stock Alpine OpenRC networking service.

Status:

- Postponed again.  The default generated-service Alpine image has been
  restored to a minimal generated OpenRC policy so normal `make run` and
  generated-mode network tests are no longer forced through the stock
  localmount/provider chain while this compatibility work remains pending.
- The stock OpenRC networking path remains blocked on the `/run/openrc/deptree`
  and `depconfig` runtime-state failure.  Resume from that focused state bug
  later with a small `openat()`/`fdopen()`/truncate reproducer instead of
  another broad boot probe.
- Resume this exploration from the `/run/openrc/deptree` and `depconfig`
  runtime-state failure recorded below, not from the older fsck/localmount
  timeout.  The latest blocker is Linux/VFS/tmpfs file descriptor or
  runtime-state behavior around OpenRC's dependency cache, not DHCP, virtio-net
  link bringup, or the IP stack itself.

Problem:

- Exploration 026 allows progress by generating a Bunix-specific
  `/etc/init.d/networking` service in the Alpine rootfs.
- That is acceptable as a temporary bootstrap path, but the intended Alpine
  environment should run the real Alpine/ifupdown-ng networking service.
- The current generated service is fine for now because it keeps `make run`
  useful while networking is still being brought up, but it is explicitly a
  compatibility crutch to remove in a future slice.
- The current replacement service is a stopgap only.  It should not become the
  Bunix network-management interface or a permanent OpenRC compatibility
  contract.
- The stock service currently pulls in OpenRC dependencies such as
  `localmount` and helper programs like `mkmntdirs`, `mountinfo`, and
  `fstabinfo` that expose gaps in Bunix's VFS, mount-table, and Linux
  compatibility behavior.

Intended outcome:

- The generated Alpine rootfs keeps Alpine's stock `/etc/init.d/networking`.
- The packaged service is not rewritten, shadowed, or replaced by rootfs
  generation.  Any Bunix-specific behavior lives in standard configuration
  files, hook scripts, or lower-level compatibility fixes.
- `make run` boots through BusyBox init, runs `openrc sysinit`, `openrc boot`,
  and `openrc default`, then BusyBox init manages the `ttyS0` login respawn.
- The stock OpenRC `networking` service starts `eth0` through ifupdown-ng and
  `udhcpc`, installs the DHCP lease and default route, and does not require a
  Bunix-specific replacement service.

Constraints:

- BusyBox init, not OpenRC, manages login respawn processes.
- Do not reintroduce a blocking `bunix-login` OpenRC service.
- Do not bypass OpenRC networking with bootstrap DHCP or test-only hooks.
- Do not regress the current working default Alpine boot while this exploration
  is pending; keep the temporary generated service enabled until the real
  Alpine service has a deterministic test and a replacement path.
- Keep policy in userspace: Alpine OpenRC, ifupdown-ng, and BusyBox/Alpine
  `udhcpc` drive network configuration.
- Any Bunix-specific compatibility should be in general VFS, mount, procfs,
  sysfs, or Linux syscall behavior, not a forked networking script.
- Preserve the existing working temporary service until this exploration has a
  deterministic failing test and the stock service path is ready to replace it.

Investigation targets:

- Identify the exact helper or syscall path that causes stock OpenRC boot to
  stop after `fsck` and before the normal login respawn.
- Audit stock `localmount`, `mkmntdirs`, `mountinfo`, `fstabinfo`, and
  `/proc/self/mountinfo` expectations against Bunix VFS/procfs behavior.
- Verify whether `/etc/fstab` entries, mount table reporting, `statfs`,
  `getmntent`, `openat`, `fstat`, `readlink`, or directory mutation semantics
  are still incomplete for OpenRC.

Tasks:

- [x] Add a deterministic OpenRC boot test that preserves Alpine's stock
      `/etc/init.d/networking` and fails at the current blocking point with
      useful failure artifacts.
- [x] Add a rootfs build switch or test-only mode that disables the temporary
      generated networking service without deleting the default working path.
- [ ] Replace temporary generated OpenRC service hacks with general VFS,
      procfs, sysfs, mount-table, or Linux compatibility fixes.
- [x] Verify the packaged `openrc-run` service body is the one being executed,
      rather than a Bunix-generated script with the same path.
- [ ] Make stock `localmount` complete successfully on the Bunix Alpine image.
- [ ] Make stock Alpine `/etc/init.d/networking start` and boot runlevel
      startup complete successfully.
- [ ] Prove BusyBox init reaches `ttyS0::respawn:/bin/login` only after the
      OpenRC sysinit, boot, and default phases return.
- [ ] Verify `rc-service networking status`, `/run/openrc/started/networking`,
      `/proc/net/config`, and external IPv4 pings after login.
- [ ] Remove the generated Bunix-specific networking service from
      `tools/build-alpine-rootfs.sh` once stock Alpine networking works.
- [ ] Re-run narrow OpenRC networking tests and the relevant Alpine boot tests.

Completion criteria:

- A fresh `make run` Alpine environment uses Alpine's stock
  `/etc/init.d/networking`.
- Networking is up after login without manual intervention.
- BusyBox init owns login respawn after OpenRC returns.
- The temporary Bunix networking-service replacement is gone.

Follow-up:

- Immediately continue with `054a-ip-stack-end-to-end-connectivity.txt` after
  this exploration.  Exploration 054 proves that Alpine/OpenRC brings the
  interface up through the stock service path; 054a proves that the IP stack is
  actually usable by validating ping source/TTL correctness, DNS, and
  `wget http://example.com` end to end from guest userspace.

Implementation Notes:

- Added `ALPINE_NETWORKING_SERVICE=generated|stock` to
  `tools/build-alpine-rootfs.sh`.  The default remains `generated`, preserving
  the current working `make run` Alpine path while the stock service path is
  investigated.
- In `stock` mode, the rootfs builder leaves the packaged Alpine
  `/etc/init.d/networking` untouched and records
  `alpine_networking_service=stock` in the rootfs manifest.
- `tools/test-alpine-rootfs.sh` now checks the selected service mode.  In
  generated mode it requires the temporary `/bin/sh` helper body.  In stock
  mode it requires the packaged `#!/sbin/openrc-run` service and rejects the
  generated helper body.
- Added `make test-alpine-rootfs-stock-networking` as the deterministic
  packaging test for the stock service body.
- Added `make test-boot-alpine-stock-networking`, which builds
  `build/modules/alpine-stock-networking-disk0.sqfs` with the stock service,
  packages it into a separate VirtIO-net ESP, and runs the Alpine smoke boot
  markers without disturbing the default generated-service image.
- Added a stock-mode-only `/etc/init.d/bunix-dev` OpenRC provider that declares
  the already-mounted Bunix devfs as `dev` and `dev-mount`.  This keeps the
  generated networking-service rootfs unchanged while allowing stock OpenRC
  dependency resolution for services such as `hwdrivers`, `machine-id`, and
  `fsck` to progress without missing-provider warnings.
- A trial `/etc/fstab` root entry moved the timeout from after `fsck` into the
  visible `Checking local filesystems` step, so it was removed.  The remaining
  blocker is still in the stock OpenRC fsck/root/localmount band and should be
  solved as a general mount-table or Linux/VFS compatibility issue, not by
  adding a synthetic root fstab row.

Validation:

- `make test-alpine-rootfs-stock-networking`
- `make test-alpine-rootfs`
- `make test-boot-alpine-stock-networking` currently fails deterministically
  before login, with failure artifacts from the first run at
  `build/failures/20260710T085814Z-6344/20260710T090114Z-6344`.
- After adding the stock-mode dev provider, the narrow packaging checks pass:
  - `make test-alpine-rootfs`
  - `make test-alpine-rootfs-stock-networking`
- The stock boot probe still times out before `login:`.  The provider removes
  the previous `Service 'hwdrivers' needs non existent service 'dev'` and
  `Service 'machine-id' needs non existent service 'dev'` warnings.  The latest
  clean failure artifact is
  `build/failures/20260710T093004Z-20138/20260710T093304Z-20138`; it reaches
  OpenRC startup, mounts `/proc`, creates `/run/openrc` and `/run/lock`, caches
  dependencies, reports clock-skew warnings, and then times out before login.
- Added targeted debug controls while investigating the post-`fsck` stall:
  - `debug-serial-userspace` keeps server/userspace logs on serial after the
    Linux init handoff.
  - `debug-linux-rpc`, `debug-linux-wait`, `debug-linux-exec`,
    `debug-linux-path-taskNNN`, and `debug-linux-syscall-task=NNN` allow
    focused Linux-server and Linux-syscall tracing without enabling the full
    noisy strace path.
  - `BUNIX_BOOT_LOGIN_WAIT` and `BUNIX_BOOT_LOGIN_CAPTURE` let the boot harness
    run longer stock-OpenRC probes without changing the default test timeout.
- Added Linux-server open-file descriptions for VFS file and directory FDs.
  `openat()` now creates a per-open description, while `dup()` and `fork()`
  share that description.  This gives directory streams and regular files
  Linux-style shared offsets only for duplicated/inherited FDs, instead of
  accidentally sharing offsets through a global VFS handle table.
- A quiet stock boot probe with `BUNIX_BOOT_LOGIN_WAIT=360` still exceeded the
  old 180s login window and was interrupted after it remained at the stock
  OpenRC fsck/localmount band, confirming that this is not only serial logging
  overhead.
- Targeted `debug-linux-wait` and `debug-linux-exec` runs show BusyBox init
  waits on `/sbin/openrc boot`; OpenRC boot waits on the `/etc/init.d/fsck`
  wrapper; the wrapper execs through `/sbin/openrc-run` into the shell runner.
- Targeted syscall/path diagnostics for task 143 show the fsck wrapper is not
  asleep in `wait4`; it is actively walking OpenRC runlevel/provider state with
  repeated `faccessat`, `openat`, `getdents64`, and `newfstatat` calls.  The
  repeated paths include `bunix-dev`, `devfs`, `hwclock`, `osclock`, `swclock`,
  and `/etc/runlevels/boot/...`.
- The rootfs runlevel layout is intentionally minimal in stock-networking
  mode: `/etc/runlevels/sysinit/bunix-dev` exists to provide `dev` and
  `dev-mount`, while `/etc/runlevels/boot/networking` is the only boot
  runlevel symlink.  The repeated `boot` runlevel checks are OpenRC dependency
  resolution looking for providers/services, not a Bunix service respawn loop.
- The current blocker is therefore narrower than the first failure: stock
  OpenRC progresses through sysinit and into `/sbin/openrc boot`, but the
  OpenRC fsck/service wrapper path is still too slow or is repeatedly probing
  missing provider state through VFS.  The next fix should improve the general
  VFS/Linux path behavior that makes those probes expensive or incorrect,
  rather than adding another generated networking-service workaround.
- A later no-debug stock boot measurement was allowed to run past 390 seconds
  of host time.  It eventually printed the visible fsck `[ ok ]` line but still
  did not advance to networking, default runlevel, or login.  This rules out a
  small timeout adjustment; the service wrapper is effectively stuck or doing
  unbounded OpenRC state/path probing after fsck succeeds.
- Added and tested a conservative VFS symlink-resolution cache keyed by
  subject task and absolute path.  The cache only stores fully-followed
  resolution results and is cleared on VFS mutations and mount changes.  This
  reduces repeated prefix symlink walks, but it is not sufficient to get the
  stock OpenRC boot to `login:` inside the current 180 second stock-networking
  boot test.
- A targeted userspace-serial debug boot with `debug-linux-wait`,
  `debug-linux-exec`, and late path tracing shows the post-`fsck` slowdown is a
  fork/exec storm of tiny OpenRC helpers, especially
  `/usr/libexec/rc/bin/shell_var`.  Each helper itself exits quickly, but the
  parent shell/OpenRC wrapper exits with status 1 and OpenRC continues to the
  next dependency/provider query.  This is normal OpenRC behavior made
  pathological by Bunix's current per-process/per-exec cost.
- A trial SquashFS regular-file data-block cache was rejected.  Caching full
  128 KiB data blocks on first read made the stock boot slower before the fsck
  phase, because OpenRC dependency generation touches many tiny service/helper
  files.  Do not reintroduce a first-touch full-block SquashFS cache without a
  narrower hot-block policy or measured evidence that it helps.
- A trial proc-server ELF segment batching change was also rejected.  Raising
  the proc loader buffer to 64 KiB and calling `task_map` on larger aligned
  chunks reduced the theoretical syscall/RPC count, but it regressed both the
  synthetic shell boot and the stock OpenRC probe.  The synthetic image stopped
  after `/bin/first` printed `first: exit 0`, and the stock Alpine image timed
  out before the visible fsck phase.  Keep the page-at-a-time loader until the
  lower-level task-map/process-exit interaction is understood.
- Added `debug-proc-exec-timing` to measure proc-server loader phases.  A
  stock OpenRC capture with this flag shows `shell_var` and `fstabinfo` exec
  image loading completes in 0-2 timer ticks; the visible 1-2 second gaps occur
  before the helper reaches `execve`, while the parent shell/OpenRC wrapper is
  running script logic and Linux syscalls.  This rules out proc-server ELF
  mapping as the dominant post-`fsck` cost.
- The next likely fix is therefore in Linux syscall/VFS behavior exercised by
  the parent shell/OpenRC scripts, or in a narrower path/stat cache.  Reduce
  repeated shell-side service/dependency probing cost without replacing the
  stock scripts or turning cold reads of many small OpenRC files into full-block
  reads.
- A trial Linux signal-pending optimization was rejected.  Narrowing the
  kernel's post-syscall signal-pending RPC to selected syscall classes reduced
  stock OpenRC `signal_pending` volume in debug captures, but it broke the
  synthetic shell path: `BUNIX_SHELL_PART=linux-signaltest make
  test-shell-part` stopped after `/bin/first` exited and never reached the
  login prompt.  The current Linux/SIGCHLD contract still depends on broad
  post-syscall signal polling, so this should not be reduced without first
  adding an explicit wakeup/pending-signal mechanism between linux.server and
  blocked Linux tasks.
- While investigating the slowdown, a clean-HEAD rebuild of `proc.server` and
  `linux.server` still reproduced the synthetic shell stop-after-first-exit
  failure with `BUNIX_SHELL_PART=linux-signaltest make test-shell-part`; latest
  artifact from this turn:
  `build/failures/20260710T204030Z-17038/20260710T204150Z-17038`.  Resolve or
  re-baseline that failure before relying on the shell shard as a guard for
  stock OpenRC performance changes.
- The synthetic shell guard regression was resolved without changing the
  intended login ownership model.  `proc.server` now accepts
  `BUNIX_PROC_EXIT` from a native task for its own process record, while still
  requiring the management capability for killing some other task.  This lets
  native one-shot boot spawns report completion instead of leaving bootstrap
  stuck waiting.
- A trial bootstrap `::respawn:/bin/login` entry was rejected.  BusyBox init
  already starts the synthetic `/etc/inittab` login while `/etc/spawns` is
  still running one-shot smoke programs, so bootstrap-level login supervision
  creates a second login that races for serial input.  Keep login respawn owned
  by BusyBox init, matching the Alpine constraint.
- The login prompt storm was caused by `/bin/login` treating empty reads as an
  immediate reason to print another prompt.  Login now waits and backs off
  through the time service when `read(0, ...)` returns no input, so it prints a
  prompt once and waits for console input instead of consuming later shell
  commands as a password.
- The synthetic shell guard is usable again:
  `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 5
  seconds after the proc/login fixes, and passed again in 65 seconds after a
  full harness rebuild with the login fallback-yield path.
- A fresh post-reboot stock OpenRC debug probe reproduced the slow path with
  current artifacts at
  `build/failures/20260710T211208Z-30499/20260710T211508Z-30499`.  The late
  trace is still one OpenRC process repeatedly issuing `faccessat()` and
  `openat()` against service/provider names such as `devfs`, `bunix-dev`,
  `hwclock`, `osclock`, `swclock`, `dmesg`, and `modloop`.
- Added defensive directory-iteration hardening while narrowing the OpenRC
  stall:
  - tmpfs live-list directory scans and subtree rename collection are bounded
    by `TMPFS_TREE_WALK_LIMIT` so a corrupt live list fails closed instead of
    wedging a server.
  - unionfs merged-directory scans now have an explicit readdir iteration
    limit and propagate limit errors instead of treating them as successful
    entries.
  - Linux `getdents64` now rejects VFS replies that do not advance the
    directory offset.
  - devfs and sysfs now return `index + 1` in the low VFS readdir result word,
    matching the VFS cookie contract used by tmpfs/procfs/squashfs instead of
    returning device/node handles as directory offsets.
- Narrow validation for the readdir hardening passed:
  - `make build/modules/tmpfs.server build/modules/unionfs.server build/modules/linux.server`
  - `make build/modules/devfs.server build/modules/sysfs.server`
  - `BUNIX_CMD=/bin/getdentstest make test-command`
  - `BUNIX_CMD=/bin/fcntllocktest make test-command`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part`
- A focused stock OpenRC probe with task 25 syscall/path tracing still did not
  reach `login:`, but it proved movement past the previous procfs-stage
  directory read.  In artifact
  `build/failures/20260711T042240Z-866/20260711T042651Z-866`, the
  `/run/openrc/scheduled` scan for `procfs` returns from `getdents64` with
  zero bytes at 215.79s, then OpenRC starts the next `/etc/init.d/procfs`
  wrapper.
- The same class of stall was later observed while scanning for `sysfs`.
  Added task-gated Linux-server `getdents64` fd diagnostics under the existing
  `debug-linux-path-taskNNN` controls and a VFS `debug-vfs-readdir` forwarding
  trace.  The focused diagnostic artifact
  `build/failures/20260711T043425Z-5279/20260711T043836Z-5279` shows fd 17 is
  `/run/openrc/scheduled`, local VFS handle 433, offset 2.  A follow-up VFS
  trace at `build/failures/20260711T044116Z-8369/20260711T044516Z-8369`
  shows `/run/openrc/scheduled` forwarding to service 10 with remote handle 52
  and returning `BUNIX_VFS_ERR_NOENT` for index 0 in the observed sample.
  This narrows the next blocker to the remaining OpenRC provider-state scan or
  a later repeated readdir path, not the earlier procfs `getdents64` call.
- Added a conservative Linux-server `faccessat()` result cache keyed by process,
  VFS base handle, path, mode, and flags.  The cache is invalidated by a global
  VFS mutation epoch bumped on create, remove, link, rename, chmod, chown,
  truncate, mount, and unmount operations.  This reduces repeated Linux-to-VFS
  access RPCs without caching across filesystem mutations.
- The access cache is not sufficient by itself.  A normal stock-networking boot
  still timed out before `login:` with artifacts at
  `build/failures/20260710T212042Z-974/20260710T212342Z-974`.
- The stock networking service declares `need localmount hostname` and orders
  after `bootmisc`, `hwdrivers`, and `modules`, while `localmount` pulls in
  `root`, `fsck`, and a `clock` provider.  The previous stock-networking policy
  enabled only `networking`, which forced OpenRC to resolve a graph full of
  absent providers.  The policy now enables the stock provider chain in boot
  using `osclock` as the lightweight clock provider and exposes `bunix-dev` to
  boot dependencies as well as sysinit.
- The runlevel-policy change packages correctly:
  `make test-alpine-rootfs-stock-networking` passed.  A boot probe after adding
  the provider chain still timed out before `login:` at
  `build/failures/20260710T212521Z-3051/20260710T212821Z-3051`, and a debug
  probe still shows repeated provider probing at
  `build/failures/20260710T212844Z-4237/20260710T213144Z-4237`.
- Stock-networking rootfs generation now installs Bunix OpenRC provider stubs
  for `devfs`, `procfs`, and `sysfs` in the `sysinit` runlevel.  These stubs
  represent filesystems Bunix already provides through VFS and avoid running
  Alpine's stock mount scripts for pseudo-filesystems that are not policy
  decisions.  This also gives dependencies such as `hwdrivers need sysfs dev`
  concrete providers instead of forcing repeated missing-provider searches.
- The provider-stub packaging was validated serially:
  - `make test-alpine-rootfs-stock-networking`
  - `make test-alpine-rootfs`
- Narrow guards after the access-cache change:
  `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 78
  seconds, and `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed
  in 5 seconds.
- Added a shared per-process Linux path metadata cache by extending the
  existing access-cache ring to also hold `newfstatat()` results.  The cache is
  still invalidated by the Linux server's VFS mutation epoch, so it is a
  correctness-preserving improvement but not sufficient for OpenRC boot by
  itself.  The current OpenRC path mutates `/run/openrc` often enough that
  immutable `/etc` provider probes still lose cache locality.
- Implemented real path-based `utimensat()` plumbing instead of the previous
  Linux-server stub that only validated the target with `newfstatat()`.
  `utimensat()` now forwards atime/mtime seconds from the x86 Linux syscall
  trampoline to `linux.server`, through VFS and unionfs, and into tmpfs inode
  timestamps.  This fixes a real OpenRC compatibility gap around
  `/run/openrc/deptree` clock-skew recovery, although it did not by itself get
  the stock boot to `login:`.
- Reworked the stock-mode Bunix devfs OpenRC provider to use Alpine's normal
  `/etc/init.d/devfs` service name and `sysinit/devfs` runlevel slot, while
  still declaring `provide dev dev-mount`.  The earlier `bunix-dev` synthetic
  provider added another provider name for OpenRC to resolve and was noisier
  than using the stock devfs compatibility point.
- Current validation after the `utimensat()` and devfs-provider changes:
  - `make`
  - `make build/modules/linux.server`
  - `make build/modules/vfs.server`
  - `make build/modules/tmpfs.server`
  - `make build/modules/unionfs.server`
  - `make test-alpine-rootfs`
  - `make test-alpine-rootfs-stock-networking`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 3
    seconds after a full rebuild.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 92
    seconds when run alone.
- A parallel run of `path-limits-statfs` and `linux-signaltest` hit the known
  shared build artifact race while linking `bootstrap.server`, producing a
  transient `undefined reference to main`.  The shard passed when rerun
  serially; do not treat that parallel build failure as a guest regression.
- The stock boot probe still times out before `login:` after these fixes, with
  latest artifacts at
  `build/failures/20260710T220507Z-20911/20260710T220808Z-20911`.
  A targeted debug run at
  `build/failures/20260710T220003Z-18602/20260710T220303Z-18602`
  shows sysinit eventually exits and BusyBox init then starts
  `/sbin/openrc boot` as `pid=119 task=142`.  The remaining hot loop is still
  OpenRC boot repeatedly probing provider/service names through
  `faccessat()`/`openat()`, especially `devfs`, `bunix-dev`, `hwclock`,
  `swclock`, `modloop`, and `dmesg`.  After the provider rename, future debug
  should confirm whether `bunix-dev` disappears from the hot path; if the
  remaining scan is mostly immutable `/etc` state, the next fix should narrow
  path-cache invalidation by mount or path class instead of clearing all Linux
  path caches for unrelated `/run` mutations.
- Added a Linux-server-global path metadata cache for normalized `/etc` and
  `/run` paths, with separate mutation epochs for `/etc`, `/run`, and other
  paths.  VFS FDs now remember their normalized path when known, so
  dirfd-relative `faccessat()` and `newfstatat()` calls such as
  `newfstatat(initd_fd, "networking", ...)` can share cached metadata across
  OpenRC helper processes without guessing about arbitrary directory FDs.
- Narrow guards after the FD-aware metadata cache pass:
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 65
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 96
    seconds.
- The FD-aware metadata cache is not sufficient to complete stock OpenRC boot.
  A normal stock-networking boot still timed out before `login:` at
  `build/failures/20260710T224045Z-1804/20260710T224346Z-1804`.
  A short debug capture at
  `build/failures/20260710T224359Z-2819/20260710T224444Z-2819`
  still shows a fork/exec-heavy `gendepends.sh` path: small helpers such as
  `/usr/libexec/rc/bin/shell_var` exec quickly and exit, but each parent
  service wrapper spends roughly 1-2 seconds before spawning the helper and
  then exits with status 1.  This points away from plain metadata lookup as
  the dominant cost.
- The next likely stock-OpenRC blocker is Linux process creation cost.  The
  x86_64 Linux syscall path recognizes `vfork()` and `clone(CLONE_VFORK)`,
  but it still creates the child through `server_task_fork_current_stopped()`,
  which clones every user VM region before the parent blocks.  OpenRC's
  shell-heavy startup pays that full VM clone cost repeatedly.  A real vfork
  optimization will need explicit task VM ownership/detach semantics so the
  child can run in the parent's address space until `execve()` without letting
  child exec teardown destroy the parent's VM space.
- Implemented borrowed-VM task support for Linux `vfork()` and
  `clone(CLONE_VFORK)`.  Vfork children now run in the parent's VM space until
  `execve()` or exit; `task_clear()` detaches a borrowed child onto a fresh VM
  before proc maps the replacement image, and borrowed children do not tear
  down the parent's VM on exit.  This preserves the existing Linux vfork wait
  path while avoiding full VM-region cloning for true vfork users.
- Narrow vfork/process guards pass after the borrowed-VM change:
  - `make`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 64
    seconds.
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 105
    seconds and covers the synthetic `vforkstress` binary.
- The borrowed-VM vfork change does not complete stock OpenRC boot by itself.
  `make test-boot-alpine-stock-networking` still timed out before `login:` at
  `build/failures/20260710T225805Z-10068/20260710T230105Z-10068`.
- A shorter debug stock probe with `log=debug debug-serial-userspace
  debug-linux-wait debug-linux-exec debug-linux-paths` timed out at
  `build/failures/20260710T230147Z-11250/20260710T230307Z-11250`.  It proves
  the new vfork path is exercised: task 27 vforks task 35 for
  `/usr/libexec/rc/bin/mountinfo`, the child execs in under a second, and the
  parent resumes after exec as expected.  The remaining visible cost is a
  plain `fork()` path: task 27 forks task 34 at 65.35s, and that child spends
  roughly 11.5 debug-log seconds running shell/helper file scans before
  exiting.  The next fix should focus on full-fork/subshell cost or the
  repeated VFS/mmap/read behavior in those stock OpenRC mount helpers, not on
  vfork cloning.
- A trial Linux-server exact small-file read cache was rejected.  The cache
  stored path+offset+length results for repeated `/etc`, `/usr`, `/bin`,
  `/sbin`, and `/lib` reads and passed focused shell guards, but the debug
  stock probe showed cache hits could not write into the caller's read buffer:
  the Linux server only holds send/dup rights to that buffer, producing
  `sched: buffer handle denied task=23 handle=27 need=0x2 rights=0x5` before
  falling back to VFS.  Do not reintroduce a userspace file-read cache unless
  the buffer capability direction is fixed or the read path is redesigned so
  the cache can legally return data to the caller.
- Kept the useful correctness piece from that experiment: successful
  Linux-server regular-file writes now call `linux_vfs_note_mutation()` for
  the normalized FD path.  This keeps the existing path/stat caches coherent
  when a file is modified through `write(2)`.
- Validation for the write-invalidation fix:
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 91
    seconds.
- A normal stock-networking boot after the rejected cache experiment still
  timed out before `login:` at
  `build/failures/20260710T231315Z-15756/20260710T231615Z-15756`.  The debug
  artifact for the rejected cache attempt is
  `build/failures/20260710T231624Z-16604/20260710T231744Z-16604`.
- Reintroduced the Linux-server exact small-file read cache after fixing the
  underlying buffer authority problem.  Linux read and mmap read buffers now
  grant the Linux server both send and receive rights, which matches the real
  flow: VFS writes file data into the buffer, the Linux server reads the
  buffer to populate a cache entry, and later cache hits write data back into
  the caller's buffer.  The cache remains conservative: exact path, offset, and
  length matches only; maximum 4096 byte reads; immutable-looking `/etc`,
  `/usr`, `/bin`, `/sbin`, and `/lib` paths only; and mutation-epoch checks.
- Validation for the fixed read-cache slice:
  - `make`
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 108
    seconds.
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 91
    seconds.
- A short stock OpenRC debug probe after the buffer-rights fix no longer
  reports the previous `sched: buffer handle denied ... need=0x2 rights=0x5`
  cache failure.  The artifact is
  `build/failures/20260710T232756Z-21200/20260710T232916Z-21200`.
- The read-cache slice does not complete stock OpenRC boot by itself.  A
  normal stock-networking boot still timed out before `login:` at
  `build/failures/20260710T232921Z-21621/20260710T233222Z-21621`.
- A longer focused debug probe with the fixed read cache confirms that
  `/usr/libexec/rc/bin/mountinfo` exits; the remaining path is still
  shell/OpenRC process churn.  Around 150 seconds, a shell reads
  `shell_var` output one byte at a time, waits for the helper, then continues
  into another plain `fork()` at roughly 158 seconds.  The artifact is
  `build/failures/20260710T233850Z-25460/20260710T234130Z-25460`.
- Added PMM frame refcounts and a conservative VM sharing helper so full
  Linux `fork()` can share non-writable file-backed regions instead of copying
  every ELF/text page eagerly.  Writable mappings, stacks, heaps, and
  anonymous mappings are still copied, preserving normal fork isolation until
  a real copy-on-write design exists.
- Narrow guards for the fork-sharing slice pass:
  - `make`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 97
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 97
    seconds.
- The fork-sharing slice does not complete stock OpenRC boot by itself.  A
  normal stock-networking boot still timed out before `login:` at
  `build/failures/20260710T234723Z-28674/20260710T235024Z-28674`.
- A local trial that skipped the post-syscall signal-pending RPC after
  immediately-ready `poll()`/`ppoll()` returns was rejected.  It preserved
  `BUNIX_SHELL_PART=linux-signaltest`, but regressed
  `BUNIX_SHELL_PART=exec-argv-pipe`, which stopped before the normal login
  prompt after `phdrstress: ok`.  The artifact is
  `build/failures/20260711T000044Z-32715/20260711T000204Z-32715`.
  Do not narrow post-syscall signal polling again without first changing the
  signal-wakeup contract.
- Fixed the stock-networking boot test harness so `QEMU_TIMEOUT=... make
  test-boot-alpine-stock-networking` is honored.  The target still defaults to
  the previous 180 second timeout, but longer OpenRC probes can now set both
  `QEMU_TIMEOUT` and `BUNIX_BOOT_LOGIN_WAIT` consistently.
- Added low-noise aggregate Linux instrumentation for the stock OpenRC path:
  - `debug-linux-counts` counts Linux-server message types and can report the
    server-side RPC/message mix without enabling per-call logs.
  - `debug-linux-syscall-counts` counts all x86_64 Linux syscalls in the
    kernel trampoline, including scalar syscalls that never become
    `linux.server` RPCs.
- A bounded stock-networking probe with `debug-linux-syscall-counts` still
  timed out before `login:` at
  `build/failures/20260711T002727Z-12183/20260711T002848Z-12183`, but it
  produced useful aggregate data without full strace noise.  By 5,000 Linux
  syscalls, the top calls were `rt_sigprocmask=921`, `read=766`,
  `mmap=446`, `close=380`, `fcntl=332`, `ppoll=323`, `munmap=289`,
  `dup2=203`, `open=199`, and `stat=153`.  By 6,000 syscalls the shape was
  the same: `rt_sigprocmask=1165`, `read=942`, `mmap=495`, `close=460`,
  `ppoll=418`, `fcntl=414`, `munmap=324`, `dup2=264`, `open=215`, and
  `stat=185`.
- This shifts the next performance target away from only VFS metadata lookup:
  OpenRC is spending heavily on signal-mask churn, byte-oriented reads,
  `ppoll`, descriptor manipulation, and mmap/munmap activity around shell
  helper execution.  Future fixes should measure those paths directly before
  adding more path caches.
- Tightened the stock OpenRC runlevel policy after the provider-probing
  diagnostics.  The boot runlevel now keeps the real dependency chain needed
  by stock networking (`osclock`, `fsck`, `root`, `localmount`, `hostname`,
  `bootmisc`, and `networking`) but suppresses Alpine's `modules` and
  `hwdrivers` services.  Stock networking only orders after those services;
  Bunix does not currently provide a Linux module loader or Linux coldplug
  device model for them to operate on, and enabling them expands OpenRC's
  provider graph without moving the stock networking outcome closer.
- Validation for the runlevel-policy tightening:
  - `make test-alpine-rootfs` passed.
  - `make test-alpine-rootfs-stock-networking` passed when rerun serially.
    The first concurrent run raced the shared rootfs artifact/cache directory
    and failed in `apk` before falling back to usermode, matching the known
    packaging-test isolation issue.
  - The generated stock-networking runlevel manifest contains only
    `boot/bootmisc`, `boot/fsck`, `boot/hostname`, `boot/localmount`,
    `boot/networking`, `boot/osclock`, `boot/root`, and the Bunix-provided
    `sysinit/devfs`, `sysinit/procfs`, and `sysinit/sysfs` pseudo-filesystem
    providers.
  - `QEMU_TIMEOUT=260s BUNIX_BOOT_LOGIN_WAIT=220
    BUNIX_BOOT_LOGIN_CAPTURE=380 make test-boot-alpine-stock-networking`
    still timed out before `login:`, with artifact
    `build/failures/20260711T051558Z-19708/20260711T051938Z-19708`.
    The visible tail is still OpenRC's dependency-cache/clock-skew/deptree
    path, so this cleanup reduces the provider graph but does not complete
    the stock boot.
- Validation for the syscall-count instrumentation:
  - `make`
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 64
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 108
    seconds.
- Added a focused `rt_sigprocmask` shape counter under
  `debug-linux-syscall-counts`.  A stock debug probe at
  `build/failures/20260711T003516Z-14793/20260711T003636Z-14793` showed no
  get-only mask calls in the sampled window.  At 1,280 `rt_sigprocmask`
  calls the shape was `block=568`, `unblock=23`, `setmask=689`, with no
  unsupported `how` values.  This confirmed the hot path is mask mutation,
  not simple mask queries.
- Added a narrow no-`oldset` `rt_sigprocmask` fast path in the x86_64 Linux
  syscall layer.  Mask mutations that do not request the old mask are now sent
  one-way to `linux.server`; calls that need `oldset` still wait for the
  authoritative server reply.  The mandatory post-syscall signal-pending RPC
  still serializes behind the queued mask update before signal delivery, so
  this does not repeat the rejected signal-polling shortcut.
- Validation for the async no-`oldset` sigmask slice:
  - `make`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 63
    seconds.
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 94
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 89
    seconds.
- A quiet stock-networking probe after the sigmask fast path still timed out
  before `login:` at
  `build/failures/20260711T004308Z-17823/20260711T004528Z-17823`.  A debug
  follow-up at
  `build/failures/20260711T004558Z-18802/20260711T004738Z-18802` showed
  OpenRC helper churn continues after the clock-skew phase, reaching 9,000
  Linux syscalls and process grants past id 115 by the 100 second login wait.
  The remaining `rt_sigprocmask` cost now includes many calls that need
  `oldset`; further reduction likely needs a kernel-side mirror of the Linux
  signal mask, copied on fork and synchronized with server-side signal
  dequeue/fault handling, instead of more one-way shortcuts.
- A trial kernel-side Linux signal-mask mirror was rejected.  The mirror
  stored the mask in `struct task`, copied it across fork/vfork, returned
  `oldset` locally, and updated it on signal delivery and sigreturn.  It built
  and `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed, but
  `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` regressed and
  stopped after `phdrstress: ok`, with artifact
  `build/failures/20260711T005431Z-22147/20260711T005551Z-22147`.  The trial
  was reverted and the same shard passed again in 102 seconds.  Do not
  reintroduce a mirrored mask without a dedicated regression test that covers
  exec/fork handoff through the post-`phdrstress` login path.
- Added focused read-shape instrumentation under `debug-linux-syscall-counts`
  in both the x86 Linux syscall path and `linux.server`.  A bounded
  stock-networking probe at
  `build/failures/20260711T010653Z-26284/20260711T010833Z-26284`
  confirmed that the hot one-byte reads are regular-file reads, not console,
  pipe, or socket reads.  At 1,536 sampled `read(2)` calls, the shape was:
  `len1=722`, `len17_256=121`, `len257_4096=692`, `fd0=722`, `fd4=200`, and
  `fd7p=582`; the server-side FD split was `file=1279`, `pipe=257`,
  `console=0`, `socket=0`, with `len1_file=722`.  This rules out terminal
  input and pipe wakeups as the dominant read-side cost in the sampled OpenRC
  boot path.  The remaining read hot spot is byte-oriented regular-file
  consumption by shell/OpenRC helper logic.
- Added a conservative per-open-file read-ahead buffer in `linux.server` for
  small sequential regular-file reads on immutable-looking paths.  The buffer
  is attached to the shared open-file description, so dup/fork share it with
  the shared Linux file offset; it is mutation-epoch checked and cleared on
  writes, truncates, and close.  This targets the measured fd0 one-byte
  regular-file reads without changing pipe, socket, console, or directory
  behavior.
- Validation for the read-ahead slice:
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 88
    seconds.
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 86
    seconds.
  - `QEMU_TIMEOUT=180s BUNIX_BOOT_LOGIN_WAIT=150 BUNIX_BOOT_LOGIN_CAPTURE=240
    make test-boot-alpine-stock-networking` still timed out before `login:`,
    with artifact
    `build/failures/20260711T011443Z-29630/20260711T011713Z-29630`.
  - A follow-up debug probe at
    `build/failures/20260711T011749Z-30507/20260711T011929Z-30507` showed the
    new read-ahead path is active: `hits=486`, `fills=26`,
    `hit_bytes=486`, and `fill_bytes=47611` in the sampled window.  The
    stock boot still did not reach login, so the remaining blocker is broader
    OpenRC process/syscall churn, not just VFS reads for byte-oriented file
    input.
- Added additional low-noise shape counters under
  `debug-linux-syscall-counts` for `fcntl`, `ppoll`, `mmap`, and `munmap`.
  A bounded stock-networking probe at
  `build/failures/20260711T012327Z-32361/20260711T012507Z-32361`
  showed the next hot paths are not ambiguous: `ppoll` calls are all
  single-fd, infinite-timeout, immediately-ready polls; by 512 sampled poll
  calls the shape was `nfds1=512`, `timeoutinf=512`, `immediate=512`,
  `slept=0`, and `empty=0`.  `fcntl` is mostly descriptor shuffling, with
  `F_SETFD=258` and `F_DUPFD_CLOEXEC=252` out of 512 sampled calls.  `mmap`
  is mostly small anonymous writable mappings, with 512 sampled calls showing
  `anon=444`, `file=68`, `writable=423`, `len4k=396`, and `len16k=89`.
- Added an initial kernel COW path for Linux `fork()`.  Full forks now share
  private anonymous writable VMAs read-only between parent and child, while
  write faults on those VMAs copy the single faulting page.  Kernel-mediated
  writes into current user buffers now retry through the same COW path, so
  syscall output buffers and signal frames do not see false `EFAULT` on COW
  pages.  File-backed read-only sharing and `vfork()` behavior are unchanged.
- A first COW probe exposed the missing kernel-write retry: stock OpenRC
  started reporting shell syntax errors such as
  `/usr/libexec/rc/sh/init.sh: line 70: syntax error` and unterminated quotes
  in `openrc-run.sh`, with artifact
  `build/failures/20260711T013424Z-4465/20260711T013654Z-4465`.  After
  adding COW retries for `write_current_user()` and signal-frame writes, the
  syntax corruption disappeared and the stock boot returned to the prior
  timeout shape at
  `build/failures/20260711T013825Z-6437/20260711T014055Z-6437`.
- Validation for the COW slice:
  - `make`
  - `BUNIX_SHELL_PART=linux-signaltest make test-shell-part` passed in 64
    seconds before the write-retry fix.
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 89
    seconds after the write-retry fix.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 73
    seconds after the write-retry fix.
  - `QEMU_TIMEOUT=180s BUNIX_BOOT_LOGIN_WAIT=150 BUNIX_BOOT_LOGIN_CAPTURE=260
    make test-boot-alpine-stock-networking` still timed out before `login:`,
    but no longer corrupts OpenRC shell scripts.
- A targeted trace for a later `/sbin/openrc` process showed repeated
  `read(2)` calls against `/etc/rc.conf` returning a full 1024 bytes forever.
  The Linux server now clamps regular-file reads to the known file size and
  returns EOF when the open-file offset reaches that size.  This fixes a
  general Linux/VFS compatibility bug: ordinary regular-file readers can no
  longer loop forever past EOF just because the lower VFS layer returned a full
  transfer.
- With the EOF clamp in place, the targeted stock trace reaches the end of the
  OpenRC boot process and starts the login-side task around 149 seconds, so the
  infinite `/etc/rc.conf` read loop is no longer the current blocker.
- Narrow guards for the EOF-clamp slice passed:
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part`
- A normal stock-networking boot probe still timed out before `login:` with
  artifacts at
  `build/failures/20260711T020607Z-16513/20260711T020948Z-16513`.  The next
  debugging step should focus on why the stock boot still does not produce the
  visible login prompt reliably after OpenRC progress improves, rather than on
  the fixed regular-file EOF loop.
- A focused wait/exec trace after the EOF clamp shows the next late stall in
  `/etc/init.d/osclock`: OpenRC starts the `osclock` `openrc-run` process as
  `pid=118 task=140`, the shell runner child exits, and then task 140 spends
  the remaining test window reading and walking OpenRC runtime state.  The
  task-specific syscall trace identifies `/run/openrc/deptree` as the next hot
  generated regular file.
- A trial Linux read-cache extension for `/run/openrc` plus a larger 4 KiB
  per-open read-ahead trigger passed focused shell guards but was rejected.  It
  did not move the normal stock-networking boot probe past the visible
  clock-skew timeout, with artifacts at
  `build/failures/20260711T023002Z-25475/20260711T023342Z-25475`.  This points
  back to broader OpenRC dependency/runtime scanning and process/syscall cost,
  not just uncached 1024-byte reads from `deptree`.
- A focused debug run found one concrete tmpfs correctness issue in the stock
  OpenRC state path: `unlinkat()` of `/run/openrc/starting/procfs` entered
  tmpfs `file_unlink()` and then did not return while removing the file from
  the global path tree.  tmpfs no longer rebuilds the entire files tree on
  every unlink; it removes the file's existing tree node directly and uses
  bounded lookup/insert walks so corrupted or stale path-tree state cannot spin
  forever.
- Validation for the tmpfs unlink/tree slice:
  - `make build/modules/tmpfs.server`
  - `BUNIX_CMD=/bin/fcntllocktest make test-command`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 97
    seconds.
  - `QEMU_TIMEOUT=260s BUNIX_BOOT_LOGIN_WAIT=220
    BUNIX_BOOT_LOGIN_CAPTURE=380 make test-boot-alpine-stock-networking`
    still timed out before `login:`, with artifact
    `build/failures/20260711T032740Z-14947/20260711T033120Z-14947`.  The fix
    removes the observed tmpfs unlink hard hang, but it does not complete
    stock OpenRC boot by itself.
- A follow-up diagnostic showed the direct tree-removal fix was still not
  enough: `unlinkat()` of `/run/openrc/exclusive/procfs` reached tmpfs access
  checks and then wedged inside missing-path lookup.  tmpfs path lookup now
  uses an explicit live-file list rather than the mutable tree nodes.  Directory
  emptiness checks, root pruning, subtree rename collection, and `readdir` use
  the same live list, so stale tree node keys can no longer wedge normal tmpfs
  path operations.
- Validation for the tmpfs live-file index slice:
  - `make build/modules/tmpfs.server`
  - `BUNIX_CMD=/bin/fcntllocktest make test-command`
  - `BUNIX_CMD="busybox mkdir -p /union-mkdir-p/a/b && echo
    UNION_MKDIR_P_ROOT_OK" make test-command`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 110
    seconds.
  - A focused diagnostic stock boot with temporary tmpfs unlink logging showed
    task 140, `/etc/init.d/procfs`, no longer hangs in
    `/run/openrc/exclusive/procfs`; it exits cleanly at roughly 181 seconds.
    The artifact is
    `build/failures/20260711T034658Z-20986/20260711T035008Z-20986`.
  - A clean stock-networking boot still timed out before `login:` at
    `build/failures/20260711T035322Z-23384/20260711T035722Z-23384`.
  - `BUNIX_SHELL_PART=tmpfs-basic-linux-tests make test-shell-part` remains
    unresolved in this worktree.  One run timed out after
    `busybox mkdir -p /union-mkdir-p/a/b`, while a rerun timed out while the
    next `/tmp/mkdir-p` command was still being echoed.  The direct
    `union-mkdir-p` command passes, so this needs a separate harness or
    long-shard state investigation before using the whole shard as a hard
    guard for the tmpfs index change.
- Upstream OpenRC source confirms the visible
  `fopen '/run/openrc/deptree': ...` message is emitted by
  `rc_deptree_update()` only when its `do_fopenat(..., "deptree",
  O_WRONLY|O_CREAT|O_TRUNC)` wrapper returns `NULL`; `do_fopenat()` itself
  first calls `openat()` and then `fdopen()`.  This means the message does not
  prove that VFS `openat()` directly returned the displayed errno.
- Added minimal AF_UNIX datagram socket compatibility for Linux userspace
  probes.  `socket(AF_UNIX, SOCK_DGRAM|SOCK_CLOEXEC, 0)` now succeeds,
  `getsockopt(SO_TYPE)` reports `SOCK_DGRAM`, and datagram `connect()` or
  `sendto()` to missing Unix socket paths returns Linux-style path errors.
  This removes one OpenRC/syslog compatibility wart, but it is not sufficient
  to complete the stock OpenRC boot.
- Validation for the AF_UNIX datagram slice:
  - `make build/modules/linux.server`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 103
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 90
    seconds.
  - A quiet stock-networking boot still failed before login with the same
    visible `deptree`/`depconfig` dependency-cache errors, at
    `build/failures/20260711T071815Z-32601/20260711T072236Z-32601`.
- A later path-traced stock OpenRC run showed `openat()` for `deptree` and
  `depconfig` succeeding around the dependency-cache phase, even when the
  quiet run reports `fopen ... Invalid argument`.  The trace also showed
  OpenRC could continue past the initial dependency-cache phase and begin
  service wrapper execution, which points at timing/state interactions around
  stdio/open-file handling rather than a simple VFS create failure.
- Tmpfs root mounting now preserves an already-mounted tmpfs root instead of
  recycling it and pruning every child below that root.  This matches the
  OpenRC/localmount expectation that a redundant tmpfs mount attempt must not
  silently destroy runtime state such as `/run/openrc`.
- Validation for the tmpfs root-preservation slice:
  - `make build/modules/tmpfs.server`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 78
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 72
    seconds.
  - `BUNIX_USER=root BUNIX_PASSWORD=root BUNIX_PROMPT='~ # '
    BUNIX_CMD="busybox sh -c 'echo RUN_REMOUNT_OK >/run/remount-keep &&
    mount -t tmpfs tmpfs /run && cat /run/remount-keep'" make test-command`
    passed, proving a repeated root-level tmpfs mount on `/run` preserves
    existing runtime files.
- The stock OpenRC boot still is not complete.  After tmpfs root preservation,
  one quiet probe reached root login and passed the `id` and proc pipe smoke
  checks, but OpenRC still reported `deptree` regeneration failures and the
  login-side `busybox dmesg >/tmp/bunix-dmesg` redirection failed with
  `Invalid argument`; artifact:
  `build/failures/20260711T081239Z-14794/20260711T081759Z-14794`.
  A follow-up `debug-tmpfs-errors` probe did not emit tmpfs create-failure
  diagnostics before the visible `deptree` failure, so the next fix should
  instrument or repair the Linux stdio/open-file path rather than assuming
  tmpfs `CREATE_BUFFER` is returning `EINVAL`.
- A later task-25 path trace reproduced the stock OpenRC stall as a VFS
  `getdents64()` forward into tmpfs for `/run/openrc/scheduled` that did not
  return before timeout in artifact
  `build/failures/20260711T052742Z-23738/20260711T053037Z-23738`.
  Hardened tmpfs path-prefix testing so child-path classification stops at
  the candidate path's NUL terminator instead of reading past short paths when
  comparing them to long directory prefixes.
- Validation for the tmpfs prefix hardening:
  - `make build/modules/tmpfs.server`
  - `BUNIX_CMD=/bin/fcntllocktest make test-command`
  - `BUNIX_CMD="busybox mkdir -p /union-mkdir-p/a/b && echo
    UNION_MKDIR_P_ROOT_OK" make test-command`
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 102
    seconds.
  - `QEMU_TIMEOUT=260s BUNIX_BOOT_LOGIN_WAIT=220
    BUNIX_BOOT_LOGIN_CAPTURE=380 make test-boot-alpine-stock-networking`
    still timed out before `login:`, with artifact
    `build/failures/20260711T053403Z-26330/20260711T053743Z-26330`.
  - A focused follow-up trace with `debug-linux-path-task25` and
    `debug-vfs-readdir` shows `/run/openrc/scheduled` now returns promptly
    through VFS/tmpfs, but OpenRC still spends the remaining boot window doing
    repeated full scans of `/etc/init.d` and `/etc/conf.d`.  The artifact is
  `build/failures/20260711T053753Z-27514/20260711T054048Z-27514`.
    The next optimization should target Linux/VFS directory enumeration
    caching or batching for immutable rootfs directories, not another
    pseudo-filesystem provider workaround.
- Added a small Linux-server directory cache for normalized `/etc` directory
  listings.  OpenRC repeatedly scans immutable service directories such as
  `/etc/init.d`, `/etc/conf.d`, and `/etc/runlevels/*`; caching those complete
  listings avoids repeated VFS `readdir` walks while preserving correctness
  through the existing `/etc` mutation epoch.  A follow-up review found and
  fixed a handle leak in the cache-fill path: cache hits no longer allocate a
  temporary name buffer that is never closed.
- Added provisional Linux wall-clock behavior for OpenRC compatibility.
  `CLOCK_REALTIME`, `gettimeofday()`, and `time()` now return a fixed
  2026-01-01 UTC boot epoch plus monotonic uptime, while
  `CLOCK_MONOTONIC` remains monotonic.  `utimensat(UTIME_NOW)` is resolved in
  the Linux server before the VFS call so OpenRC's `/run/openrc/deptree`
  clock-skew recovery writes wall-clock timestamps rather than boot-relative
  seconds.
- Tightened tmpfs path-based truncate handling so VFS path truncate messages
  use the caller task encoded in the low half of `word3` and the truncate
  length encoded in the high half.  This fixes a real subject/length decoding
  bug in the path-truncate arm, although the current OpenRC deptree failure is
  not yet proven to go through that path.
- Hardened tmpfs subtree helpers against null paths discovered while auditing
  the stock OpenRC state path.  `file_is_child_of()` and
  `file_in_renamed_subtree()` now reject null `file->path` values before
  calling path-prefix helpers.
- Added task-gated path-result logging for `openat()` and failed
  `openat(O_TRUNC)` paths.  This remains behind the existing
  `debug-linux-paths` and `debug-linux-path-taskNNN` controls and was used to
  distinguish successful deptree opens from the remaining timing-sensitive
  stock OpenRC failure.
- The Alpine dmesg smoke in `tools/test-boot.sh` now verifies that
  `busybox dmesg` can write nonempty output to a temporary file instead of
  grepping for a specific kernel/server log line.  This keeps the smoke test
  tied to functional dmesg behavior after log routing changes.
- Narrow validation for this slice:
  - `make build/modules/linux.server build/modules/tmpfs.server`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 89
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 98
    seconds when rerun serially.  A concurrent run failed before Bunix booted
    and dropped into the UEFI shell, matching the known shared ESP/package
    race rather than a guest regression.
  - `BUNIX_CMD="busybox sh -c ': >/tmp/bunix-redir && echo REDIR_OK'" make
    test-command` passed, so basic tmpfs create/truncate shell redirection
    still works in the normal generated-service image.
- The stock-networking boot still does not meet the exploration goal.  A quiet
  probe with `QEMU_TIMEOUT=320s BUNIX_BOOT_LOGIN_WAIT=260
  BUNIX_BOOT_LOGIN_CAPTURE=420 make test-boot-alpine-stock-networking` failed
  before login at
  `build/failures/20260711T065616Z-24943/20260711T070036Z-24943`, printing:
  `fopen '/run/openrc/deptree': Invalid argument`,
  `fopen '/run/openrc/depconfig': No such file or directory`, and
  `failed to load deptree`.
- A task-gated path trace did not reproduce the early deptree `fopen` failure
  and showed the relevant task opening `deptree` and `depconfig`
  successfully before continuing through sysinit service processing.  The
  artifact is
  `build/failures/20260711T070503Z-27783/20260711T070729Z-27783`.
  This makes the remaining failure look timing-sensitive or state-exhaustion
  related rather than a simple missing `openat()` implementation.
- The next fix should make the remaining OpenRC runtime-state failures more
  diagnostic and then correct the underlying state bug.  Good candidates are:
  structured tmpfs/VFS errors for truncate/write/open failures instead of raw
  `(u64)-1`, focused logging for unsupported `fcntl()` commands that currently
  return `EINVAL`, and another quiet stock probe after the directory-cache
  handle leak fix to see whether the dmesg redirection failure still appears
  after login.
- Tightened directory creation/open behavior as a general Linux/VFS
  compatibility fix.  `tmpfs` no longer reports success for
  `BUNIX_VFS_CREATE_BUFFER` when the target already exists as a directory, and
  the Linux server rejects `openat()` of an existing directory when the caller
  requested write or truncate access.  This makes shell redirection to a
  directory fail with `EISDIR` instead of allowing the path to be treated like
  a regular file.
- Validation for the directory-open slice:
  - `make build/modules/linux.server build/modules/tmpfs.server`
  - `git diff --check`
  - `BUNIX_CMD="busybox sh -c ': >/tmp/bunix-redir && echo REDIR_OK'" make
    test-command`
  - `BUNIX_CMD='busybox sh -c "busybox mkdir -p /tmp/open-dir-test; if
    busybox sh -c '\'' : >/tmp/open-dir-test '\''; then exit 1; fi; echo
    DIR_REDIR_REJECTED"' make test-command`
  - `BUNIX_SHELL_PART=exec-argv-pipe make test-shell-part` passed in 82
    seconds.
  - `BUNIX_SHELL_PART=path-limits-statfs make test-shell-part` passed in 94
    seconds.
- The directory-open fix did not complete the stock OpenRC path.  A
  stock-networking boot probe with `QEMU_TIMEOUT=320s
  BUNIX_BOOT_LOGIN_WAIT=260 BUNIX_BOOT_LOGIN_CAPTURE=420 make
  test-boot-alpine-stock-networking` still failed before login at
  `build/failures/20260711T085549Z-32532/20260711T090009Z-32532`.  It reached
  OpenRC dependency caching and then printed `fopen
  '/run/openrc/deptree': Invalid argument`, followed by `depconfig` failures
  and a later `Bad file descriptor` report.  This confirms that the remaining
  blocker is deeper in Linux/VFS/tmpfs file descriptor or runtime-state
  behavior, not only directory write-open semantics.
- This exploration can be postponed without losing the working default Alpine
  boot path, because the generated Bunix networking service remains the
  default while `ALPINE_NETWORKING_SERVICE=stock` isolates the failing stock
  OpenRC path.  If postponed, keep this file open as the authoritative stock
  OpenRC failure record and resume from the latest `deptree`/`depconfig`
  runtime-state failure rather than restarting the investigation from the
  older fsck/localmount timeout.
- A follow-up attempt to run 054a against the generated-service image proved
  that the postponement assumption is currently invalid.  The generated image
  did not reach login before the network test command; with Bunix OpenRC
  provider scripts installed for both generated and stock networking modes, it
  still failed in dependency-cache regeneration with `fopen
  '/run/openrc/deptree': Invalid argument` and `fopen
  '/run/openrc/depconfig': No such file or directory`.  Artifact:
  `build/failures/20260711T091603Z-13124/20260711T091904Z-13124`.
  The 054a test targets are useful once Alpine reaches login again, but 054
  must remain active until this OpenRC runtime-state blocker is fixed.
- The shared OpenRC provider rootfs change keeps both packaging modes valid:
  - `make test-alpine-rootfs`
  - `make test-alpine-rootfs-stock-networking`
- Generated-service rootfs generation now uses
  `tools/alpine-generated-openrc-runlevels.policy`, which only enables the
  temporary generated networking service and the Bunix login replacement.
  Stock mode continues to use `tools/alpine-openrc-runlevels.policy` and the
  Bunix pseudo-filesystem OpenRC provider stubs.  This restores generated mode
  as the lightweight default path while preserving the stock OpenRC failure
  record for later.
- Validation for the generated/stock policy split:
  - `make test-alpine-rootfs` passed when rerun serially.
  - `make test-alpine-rootfs-stock-networking` passed.
- A generated-mode strict external ping run now reaches login and executes the
  guest network assertion script instead of failing before login in OpenRC.
  It fails at the expected IP-stack layer: four replies arrive from
  `4.2.2.1`, but TTL values are unstable, with observed values such as
  `208`, `96`, `32`, and `224` in one run.  Artifact:
  `build/failures/20260711T100011Z-32096/20260711T100620Z-32096`.
  Continue with `054a-ip-stack-end-to-end-connectivity.txt`.
- Added `/bin/openrctest` as a focused Linux userspace guard for the latest
  OpenRC runtime-state failure.  It creates `/run/openrc`, repeatedly opens
  `/run/openrc/deptree` with `O_CREAT|O_TRUNC`, wraps the FD with `fdopen()`,
  writes through stdio, closes, reopens, and verifies content; it also covers
  `/run/openrc/depconfig`.  `BUNIX_CMD=/bin/openrctest make test-command`
  passes, proving the simple `openat()` plus `fdopen()` plus truncate path is
  not enough to reproduce the visible stock OpenRC failure.  The remaining
  blocker is still a broader runtime-state/file-descriptor interaction in the
  real OpenRC dependency-cache path.
- Extended `/bin/openrctest` to mirror OpenRC's local `do_fopenat()` helper
  more closely: it opens `/run/openrc` as a directory FD, uses relative
  `openat()`, `fdopen()`, `fstatat()`, `fgets()`, and `unlinkat()` on
  `deptree` and `depconfig`, and repeats the update/load cycle.  The updated
  guard still passes with `BUNIX_CMD=/bin/openrctest make test-command`, so
  the current stock OpenRC failure is not reproduced by dirfd-relative
  `O_WRONLY|O_CREAT|O_TRUNC` plus stdio either.  Continue narrowing around
  longer-lived OpenRC descriptor state, unsupported `fcntl()` paths, or
  interactions with the full dependency-cache writer rather than treating
  basic tmpfs create/truncate as the culprit.

Notes:

- Running the generated and stock rootfs packaging tests concurrently with the
  default artifact directory is unsafe today because both use
  `build/alpine-rootfs-test` and its apk cache.  Run them serially or give one
  test a distinct `ALPINE_ROOTFS_ARTIFACT_DIR` until the packaging tests get
  per-mode artifact isolation.
- The first stock boot failure reaches:
  - `OpenRC 0.63.2 is starting up Bunix 0.1 (x86_64)`
  - `Mounting /proc ... [ ok ]`
  - `Service 'hwdrivers' needs non existent service 'dev'`
  - `Service 'machine-id' needs non existent service 'dev'`
  - `Caching service dependencies ... [ ok ]`
  - clock-skew warnings while adjusting `/run/openrc/deptree`
  It then never reaches `login:` before the 180s boot-harness timeout.
