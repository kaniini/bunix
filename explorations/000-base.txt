You are working on a series of explorations.

The current exploration is: 029-virtio-irq-driven-completions.txt

Read it, then complete tasks and mark tasks as completed as they are completed.
Commit each exploration as tasks are completed.

An exploration is considered complete when all tasks are completed, and it
passes full tests.  Prefer narrow testing strategies for individual tasks.
Provide justification in responses when running expensive tests like
'make test' or unbounded 'make test-shell'.

When an exploration is considered complete:
 - Move the exploration from ./explorations/ to ./explorations/completed/.
 - Look to see if there are any other explorations in ./explorations/,
   (other than 000-base.txt) and if so,
   move to the next unfinished exploration and update this file.

If an exploration is considered complete, but has follow-up items or
unfulfilled needs, plan a new exploration.

The goal is considered achieved when no further explorations are available.

Update task list with needs.

Use detailed commit messages.

Give credit to yourself using Co-authored-by lines.

Make granular commits, only push when explorations are completed.

Use subagents for parallelism wherever efficiency would be increased.
